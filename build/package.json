
{
  "name": "mcp-server",
  "version": "1.0.0",
  "description": "MCP Server with OAuth and SSE",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "fastapi-worker": "^0.1.0"
  },
  "devDependencies": {
    "wrangler": "^2.0.0"
  }
}
